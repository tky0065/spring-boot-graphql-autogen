package com.enokdev.graphql.autogen.error;

/**
 * Defines standardized error codes for GraphQL AutoGen.
 */
public enum ErrorCodes {

    VALIDATION_ERROR,
    INTERNAL_SERVER_ERROR,
    UNAUTHORIZED_ACCESS,
    FORBIDDEN_ACCESS,
    BAD_REQUEST,
    NOT_FOUND,
    SERVICE_UNAVAILABLE,
    DUPLICATE_ENTRY,
    INVALID_ARGUMENT,
    SCHEMA_GENERATION_ERROR,
    TYPE_RESOLUTION_ERROR,
    DATA_FETCHING_ERROR,
    UNKNOWN_ERROR
}
